{
  "name": "us-for-real-couple-wellness",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "build": "node -e \"console.log('Static Firebase-enabled app ready')\"",
    "check": "node --check public/firebase-client.js && node --check functions/index.js"
  }
}
