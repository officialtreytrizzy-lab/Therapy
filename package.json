{
  "name": "us-for-real-couple-wellness",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "ci": "npm run build && npm test"
  },
  "dependencies": {
    "@google-cloud/firestore": "^8.6.0",
    "google-auth-library": "^10.9.0",
    "nodemailer": "^9.0.3"
  },
  "engines": {
    "node": "22.x"
  },
  "type": "module"
}
