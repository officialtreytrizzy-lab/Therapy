# Cloud exercises and interaction ledger

Exercise records live under `users/{uid}/cloudExercises` for private work and `couples/{coupleId}/interactionLedger` for shared metadata. Each record includes definitionId, definitionVersion, visibility, status, expiry, correction state, helpfulness, provenance, and sanitizedPromptConsent. Private AI insight and raw responses remain owner-only.
