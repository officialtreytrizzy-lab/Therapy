# Privacy-safe notifications

Notification subjects/previews must be generic: invite waiting, session ready, homework available, check-in available. Never include private answers, session topics, conflict descriptions, dossier facts, or AI-derived themes in lock-screen-safe text. Provider credentials are configurable and mocked in tests. Delivery audit stores provider IDs, status, correlation ID, template key, timestamps, and no relationship text.
