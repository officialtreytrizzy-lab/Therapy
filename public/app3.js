function reflect(){return appLayout(`<main class="page"><div class="page-head"><div><h1 class="page-title">Private Reflection</h1><p class="page-sub">Prepare for a conversation, understand a reaction, or organize what you need.</p></div></div><div class="grid grid-2"><div class="card card-pad"><h2 class="card-title green">◇ Your private coaching space</h2><p class="muted">The Guide helps you clarify feelings, assumptions, boundaries, and requests before you bring something to your partner.</p><div class="field section-gap"><label>What is on your mind?</label><textarea id="reflectTopic" placeholder="I keep getting stuck when..."></textarea></div><button class="btn btn-primary section-gap" onclick="startReflection()">Begin Reflection</button></div><div class="card card-pad"><h2 class="card-title">What this space can help with</h2><div class="stack"><div class="item"><div><h4>Prepare, not rehearse an attack</h4><p>Turn emotional urgency into a clear, fair conversation.</p></div></div><div class="item"><div><h4>Notice your pattern</h4><p>Explore what triggers withdrawal, defensiveness, pursuit, or shutdown.</p></div></div><div class="item"><div><h4>Make a specific request</h4><p>Move from “you never” toward something your partner can understand and answer.</p></div></div></div></div></div></main>`)}
window.startReflection=function(){createSession('private_coaching',document.getElementById('reflectTopic').value.trim()||'Something I need to understand before we talk',4)}
function discover(){return appLayout(`<main class="page"><div class="page-head"><div><h1 class="page-title">Discover</h1><p class="page-sub">Conversation decks designed to help you learn each other again.</p></div></div><div class="deck-grid">${state.decks.map(d=>`<a href="/discover/${d.id}" data-link class="deck"><div class="deck-top"><span class="badge badge-green">${escapeHtml(d.category)}</span><span class="muted" style="font-size:12px">${d.count} prompts</span></div><h3>${escapeHtml(d.title)}</h3><p>${escapeHtml(d.description)}</p><div class="deck-action">Open deck →</div></a>`).join('')}</div></main>`)}
const questions={1:['What’s something small I do that genuinely makes you smile?','If we had a completely free day together, what would it look like?','What tradition from your childhood would you want us to create?'],2:['When do you feel most loved by me?','What is one way I show love that you do not always recognize?','What makes you feel emotionally safe with me?'],3:['Where do you want us to be in five years?','What dream have you been afraid to say out loud?','What does home mean to you?'],4:['When do you feel most proud of your Blackness?','How should our relationship honor the culture we come from?','What does Black joy look like in our everyday life?'],5:['What parts of gay community feel most like home to you?','What is uniquely beautiful about being in a Black gay relationship?','What did you have to unlearn about love?'],6:['What does conflict feel like in your body?','What makes repair feel sincere to you?','What topic do we avoid that needs care?'],7:['What does financial safety mean to you?','What did money represent in your childhood home?','Where do ambition and partnership support each other?'],8:['How do you define manhood for yourself?','What emotion is hardest for you to express?','What kind of man do you want to be for yourself?'],9:['When do you feel closest to me?','What kind of affection do you want more of?','What helps you feel desired without pressure?']};
function deckRoom(id){const d=state.decks.find(x=>String(x.id)===String(id));if(!d)return notFound();const idx=Number(sessionStorage.getItem('deck-'+id)||0);const qs=questions[id]||questions[1];const q=qs[Math.min(idx,qs.length-1)];const done=idx>=qs.length;return appLayout(`<main class="page"><div class="row mobile-stack"><button class="btn btn-secondary btn-small" onclick="navigate('/discover')">← All decks</button><span class="badge badge-green">${escapeHtml(d.category)}</span></div><div class="card question-card"><div class="muted">${escapeHtml(d.title)} · ${Math.min(idx+1,qs.length)} of ${qs.length}</div><div class="progress-bar section-gap"><span style="width:${Math.min(100,(idx/qs.length)*100)}%"></span></div>${done?`<div style="font-size:44px;margin:30px 0 12px">✦</div><h2 class="serif" style="font-size:38px">Deck complete.</h2><p class="muted">You made space for curiosity. Save one thing you learned as a shared memory.</p><button class="btn btn-primary section-gap" onclick="saveDeckMemory('${id}')">Save what we learned</button>`:`<div class="question">${escapeHtml(q)}</div><div class="field"><label>Your answer</label><textarea id="deckAnswer" placeholder="Answer honestly. Your partner can take their turn next."></textarea></div><div class="actions"><button class="btn btn-secondary" onclick="toast('Passed for now.')">Pass</button><button class="btn btn-primary" onclick="submitDeckAnswer('${id}')">Save & Next</button></div>`}</div></main>`)}
window.submitDeckAnswer=function(id){const v=document.getElementById('deckAnswer').value.trim();if(!v){toast('Add an answer first.');return}const idx=Number(sessionStorage.getItem('deck-'+id)||0);state.deckAnswers[id]=state.deckAnswers[id]||[];state.deckAnswers[id].push(v);save();sessionStorage.setItem('deck-'+id,String(idx+1));render()};window.saveDeckMemory=function(id){const d=state.decks.find(x=>String(x.id)===String(id));state.memories.unshift({id:Date.now(),title:'What we learned: '+d.title,content:'We completed this deck and made space for a deeper conversation.',scope:'shared',category:'Discovery',sensitivity:'low'});save();sessionStorage.setItem('deck-'+id,'0');navigate('/memories');toast('Shared memory saved.')}
function missions(){const active=state.missions.active;return appLayout(`<main class="page"><div class="page-head"><div><h1 class="page-title">Missions</h1><p class="page-sub">Small shared practices that turn intention into connection.</p></div></div><div class="card card-pad"><div class="row mobile-stack"><div><span class="badge badge-green">Active Mission</span><h2 class="serif" style="font-size:30px;margin:12px 0 5px">${escapeHtml(active.title)}</h2><p class="muted">${escapeHtml(active.description)}</p></div><div style="min-width:180px"><div class="row"><span class="muted">Progress</span><strong>${active.progress}%</strong></div><div class="progress-bar" style="margin-top:8px"><span style="width:${active.progress}%"></span></div><button class="btn btn-secondary btn-small section-gap" onclick="advanceMission()">Mark today complete</button></div></div></div><h2 class="serif section-gap" style="font-size:29px">Choose your next mission</h2><div class="grid grid-3">${state.missions.available.map(m=>`<div class="card card-pad mission-card"><div class="meta"><span class="badge badge-green">${escapeHtml(m.category)}</span><span class="badge ${m.difficulty==='challenging'?'badge-plum':'badge-amber'}">${escapeHtml(m.difficulty)}</span></div><h3>${escapeHtml(m.title)}</h3><p class="muted">${escapeHtml(m.description)}</p><div class="mission-actions"><button class="btn btn-secondary btn-small" onclick="activateMission('${m.id}')">Choose mission</button></div></div>`).join('')}</div></main>`)}
window.advanceMission=function(){state.missions.active.progress=Math.min(100,state.missions.active.progress+14);if(state.missions.active.progress===100)toast('Mission complete — beautiful work.');else toast('Progress updated.');save();render()};window.activateMission=function(id){const m=state.missions.available.find(x=>String(x.id)===String(id));state.missions.active={...m,progress:0};save();render();toast('New mission activated.')}
function progress(){return appLayout(`<main class="page"><div class="page-head"><div><h1 class="page-title">Progress</h1><p class="page-sub">Growth is not perfection. It is the pattern of returning, repairing, and trying again.</p></div></div><div class="grid grid-3"><div class="card stat"><div class="green">⌁</div><div class="big">${state.weeklyStreak}</div><div class="label">Week Streak</div></div><div class="card stat"><div class="amber">◎</div><div class="big">${state.goals.filter(g=>g.status==='active').length}</div><div class="label">Active Goals</div></div><div class="card stat"><div class="plum">♛</div><div class="big">3</div><div class="label">Milestones</div></div></div><div class="grid grid-2 section-gap"><div class="card card-pad"><h2 class="card-title">Connection trend</h2><div style="display:flex;align-items:flex-end;gap:10px;height:180px;padding-top:20px">${[61,66,63,72,69,75,78].map((v,i)=>`<div style="flex:1;text-align:center"><div style="height:${v*1.55}px;background:${i===6?'var(--green)':'#303630'};border-radius:8px 8px 2px 2px"></div><span class="muted" style="font-size:10px">W${i+1}</span></div>`).join('')}</div></div><div class="card card-pad"><h2 class="card-title">Milestones earned</h2><div class="stack"><div class="item"><div><h4>First Repair</h4><p>Completed a repair session and named a concrete next step.</p></div><span class="amber">✦</span></div><div class="item"><div><h4>Seven Days of Appreciation</h4><p>Completed an appreciation streak together.</p></div><span class="plum">✦</span></div><div class="item"><div><h4>Four-Week Return</h4><p>Stayed engaged with the relationship practice for four weeks.</p></div><span class="green">✦</span></div></div></div></div></main>`)}
function appreciations(){return appLayout(`<main class="page"><div class="page-head"><div><h1 class="page-title">Appreciations</h1><p class="page-sub">Name what is working while it is still happening.</p></div></div><div class="grid grid-2"><div class="card card-pad"><h2 class="card-title plum">✧ Send an appreciation</h2><div class="field"><label>What do you want your partner to know?</label><textarea id="appMessage" placeholder="Be specific: what did they do, and what did it mean to you?"></textarea></div><div class="field section-gap"><label>Delivery</label><select id="appDelivery"><option value="instant">Send now</option><option value="end_of_week">End of week</option><option value="random">Surprise me</option></select></div><button class="btn btn-primary section-gap" onclick="sendAppreciation()">Send Appreciation</button></div><div><h2 class="serif" style="font-size:29px">Recent notes</h2><div class="stack">${state.appreciations.map(a=>`<div class="card card-pad appreciation-card"><p style="font-size:17px;line-height:1.55">“${escapeHtml(a.message)}”</p><div class="row"><span class="muted" style="font-size:12px">${dateShort(a.createdAt)}</span><span class="badge badge-plum">${escapeHtml(a.delivery.replaceAll('_',' '))}</span></div></div>`).join('')}</div></div></div></main>`)}
window.sendAppreciation=function(){const el=document.getElementById('appMessage'),v=el.value.trim();if(!v){toast('Write the appreciation first.');return}state.appreciations.unshift({id:Date.now(),message:v,delivery:document.getElementById('appDelivery').value,createdAt:now()});save();render();toast('Appreciation saved.')}
function goals(){return listEditorPage('Goals','Shared outcomes you can build toward together.','goal')}
function agreements(){return listEditorPage('Agreements','Clear commitments that reduce guessing and support trust.','agreement')}
function memories(){return listEditorPage('Memory Bank','Consent-based context that helps the Guide understand your relationship.','memory')}
function listEditorPage(title,sub,type){let items=state[type==='goal'?'goals':type==='agreement'?'agreements':'memories'];return appLayout(`<main class="page"><div class="page-head"><div><h1 class="page-title">${title}</h1><p class="page-sub">${sub}</p></div></div><div class="grid grid-2"><div class="card card-pad"><h2 class="card-title">＋ Add ${type}</h2><div class="field"><label>Title</label><input id="editTitle" placeholder="${type==='goal'?'A shared goal':type==='agreement'?'Name the agreement':'A useful relationship detail'}"></div><div class="field section-gap"><label>${type==='agreement'?'Terms':'Details'}</label><textarea id="editDetails" placeholder="Be clear and specific."></textarea></div>${type==='memory'?`<div class="form-grid section-gap"><div class="field"><label>Scope</label><select id="editScope"><option value="shared">Shared</option><option value="private">Private</option><option value="session_only">Session only</option></select></div><div class="field"><label>Category</label><input id="editCategory" placeholder="Communication"></div></div>`:''}<button class="btn btn-primary section-gap" onclick="addEditorItem('${type}')">Save ${type}</button></div><div><h2 class="serif" style="font-size:29px">${type==='goal'?'Active goals':type==='agreement'?'Current agreements':'Saved context'}</h2><div class="stack">${items.map(x=>`<div class="item"><div style="flex:1"><div style="display:flex;align-items:center;gap:8px"><h4>${escapeHtml(x.title)}</h4>${type==='memory'?`<span class="badge ${x.scope==='shared'?'badge-green':'badge-plum'}">${escapeHtml(x.scope)}</span>`:''}</div><p>${escapeHtml(x.description||x.terms||x.content||'')}</p>${type==='goal'?`<div class="row section-gap"><div class="progress-bar" style="flex:1"><span style="width:${x.progress}%"></span></div><strong>${x.progress}%</strong></div>`:''}${type==='agreement'&&x.reviewDate?`<p style="margin-top:7px">Review ${dateShort(x.reviewDate)}</p>`:''}</div><button class="btn btn-danger btn-small" onclick="removeEditorItem('${type}','${x.id}')">Remove</button></div>`).join('')}</div></div></div></main>`)}
window.addEditorItem=function(type){const title=document.getElementById('editTitle').value.trim(),details=document.getElementById('editDetails').value.trim();if(!title||!details){toast('Add a title and details.');return}const id=Date.now();if(type==='goal')state.goals.unshift({id,title,description:details,category:'Relationship',progress:0,status:'active',targetDate:daysFromNow(30)});if(type==='agreement')state.agreements.unshift({id,title,terms:details,status:'active',reviewDate:daysFromNow(30)});if(type==='memory')state.memories.unshift({id,title,content:details,scope:document.getElementById('editScope').value,category:document.getElementById('editCategory').value||'Relationship',sensitivity:'low'});save();render();toast(type.charAt(0).toUpperCase()+type.slice(1)+' saved.')};window.removeEditorItem=function(type,id){const key=type==='goal'?'goals':type==='agreement'?'agreements':'memories';state[key]=state[key].filter(x=>String(x.id)!==String(id));save();render();toast('Removed.')}