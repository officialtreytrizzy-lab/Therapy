(function(){
'use strict';

const baseRender=render;
const CALIBRATOR_KEY='usfr-conflict-calibrator-v1';
let calibratorTimer=null;

function accountState(){return window.USFRFirebase?.getState?.()||{user:null,profile:null,assignments:[],bridgePrompts:[]}}
function profileName(){return accountState().profile?.displayName||state.profile?.displayName||'You'}
function partnerLabel(){return state.profile?.partnerName||accountState().profile?.partnerDisplayName||'the other person'}
function isLinked(){const p=accountState().profile;return Boolean(p?.coupleId&&p?.relationshipStatus==='linked')}
function route(){return location.pathname.replace(/\/$/,'')||'/'}
function cleanText(value){return String(value||'').trim()}
function reasonCount(values){return (values||[]).filter(value=>cleanText(value).length>=3).length}
function safeJson(value,fallback){try{return JSON.parse(value)}catch{return fallback}}
function defaultCalibration(){return{phase:'intro',yourReasons:['','',''],theirReasons:['','',''],selfStartedAt:0,selfSeconds:0,otherStartedAt:0,otherSeconds:0,selfTimedOut:false,otherTimedOut:false,completedAt:null}}
function loadCalibration(){return{...defaultCalibration(),...safeJson(sessionStorage.getItem(CALIBRATOR_KEY),{})}}
let calibration=loadCalibration();
function saveCalibration(){sessionStorage.setItem(CALIBRATOR_KEY,JSON.stringify(calibration))}
function resetCalibration(renderNow=true){clearInterval(calibratorTimer);calibration=defaultCalibration();saveCalibration();if(renderNow)render()}
function svgIcon(name){const icons={home:'<path d="M3 10.8 12 3l9 7.8v9.7a.5.5 0 0 1-.5.5H15v-6H9v6H3.5a.5.5 0 0 1-.5-.5z"/>',sessions:'<path d="M4 5.5h16v11H8l-4 3z"/><path d="M8 9h8M8 13h5"/>',calibrate:'<path d="M12 3v18M5 7h14M7.5 7 4 13h7zM16.5 7 13 13h7z"/>',reflect:'<path d="M12 21s7-3.5 7-10V5l-7-2-7 2v6c0 6.5 7 10 7 10z"/><path d="m9 12 2 2 4-5"/>',more:'<circle cx="5" cy="5" r="1.3"/><circle cx="12" cy="5" r="1.3"/><circle cx="19" cy="5" r="1.3"/><circle cx="5" cy="12" r="1.3"/><circle cx="12" cy="12" r="1.3"/><circle cx="19" cy="12" r="1.3"/><circle cx="5" cy="19" r="1.3"/><circle cx="12" cy="19" r="1.3"/><circle cx="19" cy="19" r="1.3"/>',spark:'<path d="m12 2 1.5 6.5L20 10l-6.5 1.5L12 18l-1.5-6.5L4 10l6.5-1.5z"/><path d="m19 16 .7 2.3L22 19l-2.3.7L19 22l-.7-2.3L16 19l2.3-.7z"/>',lock:'<rect x="5" y="10" width="14" height="11" rx="3"/><path d="M8 10V7a4 4 0 0 1 8 0v3"/>',arrow:'<path d="M5 12h14M14 7l5 5-5 5"/>'};return `<svg viewBox="0 0 24 24" aria-hidden="true">${icons[name]||icons.spark}</svg>`}
function mobileLink(path,label,icon){const active=route()===path||route().startsWith(path+'/');return `<a href="${path}" data-link class="mobile-nav-link ${active?'active':''}">${svgIcon(icon)}<span>${label}</span></a>`}
function menuLink(path,label,description,icon){const active=route()===path||route().startsWith(path+'/');return `<a href="${path}" data-link class="more-menu-link ${active?'active':''}"><span class="more-menu-icon">${icon}</span><span><strong>${label}</strong><small>${description}</small></span></a>`}

window.toggleAppMore=function(force){const sheet=document.getElementById('appMoreSheet'),veil=document.getElementById('appMoreVeil');if(!sheet)return;const open=force===undefined?!sheet.classList.contains('show'):Boolean(force);sheet.classList.toggle('show',open);veil?.classList.toggle('show',open);document.body.classList.toggle('menu-open',open)};
document.addEventListener('click',event=>{if(event.target.closest('a[data-link]'))toggleAppMore(false)},true);

appLayout=function(content){const path=route(),member=profileName(),linked=isLinked();return `<div class="app-shell premium-shell"><aside class="sidebar premium-sidebar"><div class="side-head"><a href="/dashboard" data-link class="logo"><span class="logo-main">US,</span><span class="logo-sub">FOR REAL</span></a><span class="workspace-label">${linked?'Couple workspace':'Private workspace'}</span></div><nav class="nav premium-nav">${navLink('/dashboard','Home','♡')}${navLink('/sessions','Live Sessions','◉')}${navLink('/calibrate','Conflict Calibrator','⚖')}${navLink('/reflect','Private Check-in','◇')}${navLink('/discover','Exercises & Games','✦')}${navLink('/missions','My Tasks','⚑')}${navLink('/progress','Progress','⌁')}<div class="nav-divider"></div>${navLink('/appreciations','Appreciations','✧')}${navLink('/goals','Shared Goals','◎')}${navLink('/agreements','Agreements','≋')}${navLink('/memories','Interaction History','▣')}</nav><div class="side-bottom"><a href="/settings" data-link class="profile-link"><div class="avatar">${escapeHtml(member.charAt(0)||'U')}</div><div class="profile-meta"><strong>${escapeHtml(member)}</strong><span>${linked?'Linked couple':'Solo member'}</span></div><span class="profile-chevron">›</span></a></div></aside><div class="main-wrap"><header class="mobile-head premium-mobile-head"><a href="/dashboard" data-link class="logo"><span class="logo-main">US,</span><span class="logo-sub">FOR REAL</span></a><div class="mobile-context"><span class="context-dot"></span><span>${path==='/calibrate'?'Calibrator':linked?'Couple space':'Private space'}</span></div></header>${content}</div></div><div id="appMoreVeil" class="app-more-veil" onclick="toggleAppMore(false)"></div><section id="appMoreSheet" class="app-more-sheet" aria-label="More navigation"><div class="more-sheet-handle"></div><div class="more-sheet-head"><div><span class="mini-kicker">Your space</span><h2>Everything else</h2></div><button class="more-sheet-close" onclick="toggleAppMore(false)" aria-label="Close menu">×</button></div><div class="more-menu-grid">${menuLink('/discover','Exercises & Games','Private prompts and games','✦')}${menuLink('/missions','My Tasks','Practices you choose','⚑')}${menuLink('/progress','Progress','Evidence of follow-through','⌁')}${menuLink('/appreciations','Appreciations','Name what is working','✧')}${menuLink('/goals','Shared Goals','Build toward something','◎')}${menuLink('/agreements','Agreements','Clear mutual commitments','≋')}${menuLink('/memories','Interaction History','Your app activity ledger','▣')}${menuLink('/settings','Settings','Profile, privacy, and tone','⚙')}</div><button class="account-sheet-button" onclick="toggleAppMore(false);window.USFRFirebase?.open?.()"><span>${svgIcon('lock')}</span><span><strong>Account & member ID</strong><small>Sign in, link a partner, or manage privacy</small></span><span>›</span></button></section><nav class="mobile-tabbar premium-tabbar">${mobileLink('/dashboard','Home','home')}${mobileLink('/sessions','Sessions','sessions')}${mobileLink('/calibrate','Calibrate','calibrate')}${mobileLink('/reflect','Check-in','reflect')}<button class="mobile-nav-link ${['/discover','/missions','/progress','/appreciations','/goals','/agreements','/memories','/settings'].some(p=>path.startsWith(p))?'active':''}" onclick="toggleAppMore()">${svgIcon('more')}<span>More</span></button></nav>`}

function emptyState(icon,title,text,action='',onclick=''){return `<div class="polish-empty"><span>${icon}</span><h3>${escapeHtml(title)}</h3><p>${escapeHtml(text)}</p>${action?`<button class="btn btn-secondary btn-small" onclick="${onclick}">${escapeHtml(action)}</button>`:''}</div>`}
function activeCloudSession(){return (state.cloudSessions||[]).find(item=>['waiting','active'].includes(item.status))}

dashboard=function(){const cloud=activeCloudSession(),fbData=accountState(),assignments=fbData.assignments||[],prompts=fbData.bridgePrompts||[],recent=state.sessions.slice(0,3),member=profileName();return appLayout(`<main class="page experience-page dashboard-page"><section class="dashboard-hero"><div><span class="mini-kicker">${isLinked()?'Shared relationship workspace':'Your private relationship workspace'}</span><h1>${isLinked()?'Your relationship, with room for both truths.':`Welcome back, ${escapeHtml(member)}.`}</h1><p>${isLinked()?'Slow conflict down, understand the pattern, and choose what happens next without exposing either person’s private work.':'Use private check-ins, exercises, and the conflict calibrator now. Connect a partner only when you are ready.'}</p></div><div class="hero-actions"><button class="btn btn-primary" onclick="navigate('/calibrate')">Calibrate a conflict</button><button class="btn btn-secondary" onclick="navigate('/reflect')">Private check-in</button></div></section>${cloud?`<section class="now-card"><div class="now-pulse"></div><div><span class="mini-kicker">${cloud.status==='active'?'Live now':'Waiting for partner'}</span><h2>${escapeHtml(cloud.topic||'Shared session')}</h2><p>${escapeHtml(cloud.desiredOutcome||'A synchronized session is ready on both devices.')}</p></div><button class="btn btn-primary btn-small" onclick="navigate('/sessions/cloud:${cloud.id}')">Open session</button></section>`:''}<section class="quick-grid"><button class="quick-card private-quick" onclick="navigate('/reflect')"><span class="quick-icon">◇</span><span><small>Private</small><strong>Check in with yourself</strong><p>Get clear on the moment, the feeling, and what you actually need.</p></span><span class="quick-arrow">›</span></button><button class="quick-card calibrate-quick" onclick="navigate('/calibrate')"><span class="quick-icon">⚖</span><span><small>60 seconds</small><strong>Run the Conflict Calibrator</strong><p>Build your case, switch perspectives, then decide whether you are ready to talk.</p></span><span class="quick-arrow">›</span></button><button class="quick-card shared-quick" onclick="${isLinked()?"navigate('/sessions/new')":"window.USFRFirebase?.open?.()"}"><span class="quick-icon">◉</span><span><small>${isLinked()?'Together':'When ready'}</small><strong>${isLinked()?'Start a live session':'Connect a partner'}</strong><p>${isLinked()?'Open a guided room on both devices.':'Your solo tools stay available before you link anyone.'}</p></span><span class="quick-arrow">›</span></button></section><section class="dashboard-columns"><div class="dashboard-main-column"><div class="section-heading"><div><span class="mini-kicker">Your activity</span><h2>What has happened here</h2></div><a href="/sessions" data-link>View sessions</a></div><div class="metric-row"><article><strong>${state.sessions.filter(s=>s.status==='completed').length}</strong><span>Sessions</span></article><article><strong>${state.checkins.length}</strong><span>Check-ins</span></article><article><strong>${(state.conflictCalibrations||[]).length}</strong><span>Calibrations</span></article><article><strong>${assignments.filter(a=>a.status==='active').length}</strong><span>Assignments</span></article></div><div class="card activity-card">${recent.length?recent.map(s=>`<a href="/sessions/${s.cloud?'cloud:':''}${s.id}" data-link class="activity-row"><span class="activity-mark">${s.status==='completed'?'✓':'◉'}</span><span><strong>${escapeHtml(s.topic||'Relationship session')}</strong><small>${escapeHtml(s.status||'Saved')}</small></span><span>›</span></a>`).join(''):emptyState('◇','Your space starts clean','Nothing is invented here. Activity appears only after you create it.')}</div></div><aside class="dashboard-side-column"><div class="privacy-summary-card"><span class="privacy-lock">${svgIcon('lock')}</span><span class="mini-kicker">Privacy boundary</span><h2>Your raw words stay yours.</h2><p>Private check-ins, calibrations, exercises, and assignment reflections are stored as owner-only work. A partner can receive a neutral prompt, never your quote or attribution.</p></div><div class="card prompt-card"><div class="section-heading compact"><div><span class="mini-kicker">Guide prompts</span><h2>Selected for you</h2></div></div>${prompts.length?prompts.slice(0,3).map(prompt=>`<div class="prompt-row"><span>✦</span><p>${escapeHtml(prompt.prompt||'')}</p></div>`).join(''):emptyState('✦','Nothing queued','The Guide has not selected a private prompt for you yet.')}</div></aside></section></main>`)}

reflect=function(){const fbData=accountState(),assignments=fbData.assignments||[],prompts=fbData.bridgePrompts||[];return appLayout(`<main class="page experience-page private-space-page"><section class="page-intro premium-page-intro"><div><span class="mini-kicker">Private to ${escapeHtml(profileName())}</span><h1>Slow the moment down.</h1><p>Put the facts, feelings, and need in separate places. Your wording remains in your private member space.</p></div><button class="btn btn-secondary" onclick="navigate('/calibrate')">Use Conflict Calibrator</button></section><section class="private-layout"><div class="private-form-card"><div class="form-card-head"><span class="form-step">01</span><div><span class="mini-kicker">Private check-in</span><h2>What is really happening?</h2><p>Be specific enough that the Guide can help without guessing.</p></div></div><div class="polished-field"><label for="privateEvent"><span>The moment</span><small>What happened, without the whole relationship history?</small></label><textarea id="privateEvent" placeholder="Example: We agreed to talk after dinner, but they went quiet and left the room."></textarea></div><div class="two-field-row"><div class="polished-field"><label for="privateFeeling"><span>Main feeling</span><small>Name the strongest one.</small></label><input id="privateFeeling" placeholder="Hurt, dismissed, anxious…"></div><div class="polished-field intensity-field"><label for="privateIntensity"><span>Intensity</span><small>1 calm · 10 flooded</small></label><input id="privateIntensity" type="number" min="1" max="10" value="5"></div></div><div class="polished-field"><label for="privateNeed"><span>What are you needing or protecting?</span><small>Respect, reassurance, honesty, time, autonomy, safety…</small></label><textarea id="privateNeed" placeholder="I need clarity and follow-through, not another promise made in the moment."></textarea></div><div class="polished-field"><label for="privateQuestion"><span>What do you want help understanding?</span><small>Ask one direct question.</small></label><textarea id="privateQuestion" placeholder="Am I reacting to this moment, or to a pattern I have not clearly named?"></textarea></div><div class="form-submit-row"><span>${svgIcon('lock')} Saved as owner-only input</span><button class="btn btn-primary" onclick="submitPrivateCheckin()">Ask the Guide privately</button></div><div id="privateGuideResponse" class="section-gap"></div></div><aside class="private-side-rail"><div class="side-privacy-card"><span>${svgIcon('lock')}</span><h3>What stays private</h3><p>Your event description, exact feelings, questions, and assignment reflections are not shown to a linked partner.</p></div><div class="side-list-card"><div class="section-heading compact"><div><span class="mini-kicker">Private practice</span><h2>Assignments</h2></div></div>${assignments.length?assignments.slice(0,4).map(a=>`<div class="assignment-row"><span class="assignment-status">${escapeHtml(a.status||'active')}</span><strong>${escapeHtml(a.assignment||'Private practice')}</strong>${a.status==='active'?`<div class="polished-field small"><label>How did it go?</label><textarea id="assignment-${a.id}" placeholder="This reflection stays private."></textarea></div><div class="two-field-row compact"><div class="polished-field small"><label>Completion 0–10</label><input id="rating-${a.id}" type="number" min="0" max="10" value="5"></div><button class="btn btn-secondary btn-small" onclick="completePrivateAssignment('${a.id}')">Complete</button></div>`:''}</div>`).join(''):emptyState('⚑','No assignment yet','Private assignments can be created after a completed guided session.')}</div><div class="side-list-card"><div class="section-heading compact"><div><span class="mini-kicker">Middleman support</span><h2>Questions for you</h2></div></div>${prompts.length?prompts.slice(0,4).map(p=>`<div class="prompt-row"><span>✦</span><p>${escapeHtml(p.prompt||'')}</p></div>`).join(''):emptyState('✦','Nothing queued','Neutral prompts appear here without identifying who or what inspired them.')}</div></aside></section></main>`)}

function timerSeconds(){const now=Date.now();if(calibration.phase==='self')return Math.max(0,60-Math.floor((now-calibration.selfStartedAt)/1000));if(calibration.phase==='other')return Math.max(0,60-Math.floor((now-calibration.otherStartedAt)/1000));return 0}
function timerMarkup(seconds,label){const progress=Math.max(0,Math.min(100,seconds/60*100));return `<div class="cal-timer" style="--cal-progress:${progress}%"><div><strong id="calTimerValue">${String(seconds).padStart(2,'0')}</strong><span>${label}</span></div></div>`}
function reasonsMarkup(side,values){const labels=side==='your'?[['The fact','What concrete fact supports your position?'],['The impact','What effect did this have on you?'],['The boundary','What standard or agreement matters here?']]:[['Their facts','What fact might support their position?'],['Their protection','What need, fear, or value might they be protecting?'],['Your blind spot','What might you have missed, minimized, or assumed?']];return `<div class="reason-stack">${labels.map((item,index)=>`<label class="reason-card"><span class="reason-number">0${index+1}</span><span class="reason-copy"><strong>${item[0]}</strong><small>${item[1]}</small><textarea data-cal-side="${side}" data-cal-index="${index}" oninput="updateCalibrationReason('${side}',${index},this.value)" placeholder="One clear reason…">${escapeHtml(values[index]||'')}</textarea></span></label>`).join('')}</div>`}
function recentCalibrationMarkup(){const recent=(state.conflictCalibrations||[]).slice(0,3);if(!recent.length)return '';return `<div class="cal-history"><span class="mini-kicker">Recent private calibrations</span>${recent.map(item=>`<div><span>${dateShort(item.completedAt)}</span><strong>${escapeHtml(item.summary||'Balanced perspective saved')}</strong></div>`).join('')}</div>`}
function calibrator(){const phase=calibration.phase,other=partnerLabel();if(phase==='intro')return appLayout(`<main class="page experience-page calibrator-page"><section class="cal-intro"><div class="cal-intro-copy"><span class="mini-kicker">Private solo tool · 60 seconds per side</span><h1>Conflict Calibrator</h1><p>Before you confront, text, withdraw, or over-explain, test whether you can hold your truth and another possible truth at the same time.</p><div class="cal-rule-row"><span><b>1</b> Build your case</span><span><b>2</b> Switch seats</span><span><b>3</b> Choose your next move</span></div><div class="hero-actions"><button class="btn btn-primary" onclick="startConflictCalibration()">Start the 60-second check</button><button class="btn btn-secondary" onclick="navigate('/reflect')">Use a private check-in instead</button></div></div><div class="cal-intro-orbit"><span>⚖</span><strong>Both truths<br>can exist.</strong><small>Private to your account</small></div></section>${recentCalibrationMarkup()}<section class="cal-privacy-note">${svgIcon('lock')}<span><strong>No partner connection required.</strong> Your reasons are private and are not sent to anyone.</span></section></main>`);if(phase==='pause')return appLayout(`<main class="page experience-page calibrator-page"><section class="cal-pause-card"><span class="pause-icon">◌</span><span class="mini-kicker">Pause signal</span><h1>Take some heat out of the moment.</h1><p>You reached one minute before finding three clear reasons for why you are right. That does not mean you are wrong. It can mean your feelings are fused to the position right now, creating a bias that makes the full situation harder to see.</p><div class="pause-guidance"><strong>Cool off before you act.</strong><span>Step away, breathe, drink water, or wait ten minutes before sending the message or starting the conversation.</span></div><div class="hero-actions"><button class="btn btn-primary" onclick="continueCalibrationWithAwareness()">Continue with awareness</button><button class="btn btn-secondary" onclick="resetConflictCalibration()">Pause and come back later</button></div></section></main>`);if(phase==='done'){const your=calibration.yourReasons.filter(cleanText),their=calibration.theirReasons.filter(cleanText);return appLayout(`<main class="page experience-page calibrator-page"><section class="cal-complete-head"><span class="complete-check">✓</span><span class="mini-kicker">Perspective calibrated</span><h1>You found room for more than one truth.</h1><p>This does not erase impact or excuse harm. It gives you a cleaner place to decide what needs to be said next.</p></section><section class="cal-result-grid"><article><span class="result-label yours">Your position</span>${your.map(value=>`<p>${escapeHtml(value)}</p>`).join('')}</article><article><span class="result-label theirs">Their possible position</span>${their.map(value=>`<p>${escapeHtml(value)}</p>`).join('')}</article></section><section class="cal-script-card"><span class="mini-kicker">A balanced opening</span><blockquote>“I can see why I believe <b>${escapeHtml(your[0]||'what I believe')}</b>. I can also see why you may believe <b>${escapeHtml(their[0]||'what you believe')}</b>. I want us to focus on what actually needs to change, not proving who is the worse person.”</blockquote><div class="hero-actions"><button class="btn btn-primary" onclick="navigate('/reflect')">Continue in private check-in</button><button class="btn btn-secondary" onclick="resetConflictCalibration()">Calibrate another conflict</button></div></section></main>`);}const self=phase==='self',seconds=timerSeconds();return appLayout(`<main class="page experience-page calibrator-page"><section class="cal-work-head"><div><span class="mini-kicker">${self?'Step 1 of 2 · Build your case':'Step 2 of 2 · Switch seats'}</span><h1>${self?'Give three reasons you are right.':`Give three reasons ${escapeHtml(other)} could be right.`}</h1><p>${self?'Use facts, impact, and standards. Do not repeat the same reason three ways.':`Argue the strongest fair version of their position. You are not agreeing. You are checking what you may be unable to see.`}</p></div>${timerMarkup(seconds,self?'seconds to name all 3':'seconds to switch perspective')}</section><div class="cal-progress-track"><i style="width:${self?'50':'100'}%"></i></div>${reasonsMarkup(self?'your':'their',self?calibration.yourReasons:calibration.theirReasons)}<section class="cal-action-bar"><span id="calReasonStatus">${reasonCount(self?calibration.yourReasons:calibration.theirReasons)} of 3 reasons ready</span><button id="calContinueButton" class="btn btn-primary" onclick="${self?'finishYourReasons()':'finishTheirReasons()'}" ${reasonCount(self?calibration.yourReasons:calibration.theirReasons)===3?'':'disabled'}>${self?'Switch perspectives':'See calibrated view'}</button></section></main>`)}

function updateCalibratorButton(){const side=calibration.phase==='self'?'your':'their',values=side==='your'?calibration.yourReasons:calibration.theirReasons,count=reasonCount(values),button=document.getElementById('calContinueButton'),status=document.getElementById('calReasonStatus');if(button)button.disabled=count!==3;if(status)status.textContent=`${count} of 3 reasons ready`}
window.updateCalibrationReason=function(side,index,value){const key=side==='your'?'yourReasons':'theirReasons';calibration[key][index]=value;saveCalibration();updateCalibratorButton()};
window.startConflictCalibration=function(){calibration=defaultCalibration();calibration.phase='self';calibration.selfStartedAt=Date.now();saveCalibration();render()};
window.finishYourReasons=function(){if(reasonCount(calibration.yourReasons)!==3)return toast('Add three distinct reasons before switching perspectives.');calibration.selfSeconds=Math.max(1,Math.round((Date.now()-calibration.selfStartedAt)/1000));if(calibration.selfSeconds>60){calibration.selfTimedOut=true;calibration.phase='pause'}else{calibration.phase='other';calibration.otherStartedAt=Date.now()}saveCalibration();render()};
window.continueCalibrationWithAwareness=function(){calibration.phase='other';calibration.otherStartedAt=Date.now();saveCalibration();render()};
window.finishTheirReasons=function(){if(reasonCount(calibration.theirReasons)!==3)return toast('Add three fair reasons for the other perspective.');calibration.otherSeconds=Math.max(1,Math.round((Date.now()-calibration.otherStartedAt)/1000));calibration.otherTimedOut=calibration.otherSeconds>60;calibration.phase='done';calibration.completedAt=now();state.conflictCalibrations=state.conflictCalibrations||[];state.conflictCalibrations.unshift({id:Date.now(),completedAt:calibration.completedAt,selfSeconds:calibration.selfSeconds,otherSeconds:calibration.otherSeconds,selfTimedOut:calibration.selfTimedOut,otherTimedOut:calibration.otherTimedOut,yourReasons:calibration.yourReasons.map(cleanText),theirReasons:calibration.theirReasons.map(cleanText),summary:`Held ${reasonCount(calibration.yourReasons)} reasons for self and ${reasonCount(calibration.theirReasons)} for the other perspective`,private:true});state.conflictCalibrations=state.conflictCalibrations.slice(0,50);save();saveCalibration();render()};
window.resetConflictCalibration=function(){resetCalibration(true)};

function runCalibratorClock(){clearInterval(calibratorTimer);if(!['self','other'].includes(calibration.phase))return;calibratorTimer=setInterval(()=>{const seconds=timerSeconds(),value=document.getElementById('calTimerValue'),timer=document.querySelector('.cal-timer');if(value)value.textContent=String(seconds).padStart(2,'0');if(timer)timer.style.setProperty('--cal-progress',`${Math.max(0,Math.min(100,seconds/60*100))}%`);if(seconds>0)return;if(calibration.phase==='self'&&reasonCount(calibration.yourReasons)<3){calibration.selfSeconds=60;calibration.selfTimedOut=true;calibration.phase='pause';saveCalibration();clearInterval(calibratorTimer);render();return}if(calibration.phase==='other'){calibration.otherTimedOut=true;saveCalibration();const label=timer?.querySelector('span');if(label)label.textContent='time is up · finish fairly'}} ,250)}

function afterRender(){document.body.dataset.route=route().slice(1)||'home';requestAnimationFrame(()=>{runCalibratorClock();const c=document.getElementById('chatMessages');if(c)c.scrollTop=c.scrollHeight})}
render=function(){if(route()==='/calibrate'){document.getElementById('app').innerHTML=calibrator();afterRender();return}baseRender();afterRender()};
window.render=render;
window.addEventListener('popstate',()=>{if(route()==='/calibrate')render()});
render();
})();
